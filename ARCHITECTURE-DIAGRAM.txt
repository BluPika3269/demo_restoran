╔══════════════════════════════════════════════════════════════════════════════╗
║                     NOKTI SALON - ARHITEKTURA FIX                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│  PRIJE (BROKEN) ❌                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

┌───────────────────┐
│   Vercel Client   │  Frontend pokušava zvati:
│   Deployment      │  NEXT_PUBLIC_API_URL/categories
└─────────┬─────────┘
          │
          │ HTTP Request
          ▼
┌───────────────────┐
│   Vercel Server   │  ❌ NE POSTOJI!
│   Deployment      │  "deployment not found"
└─────────┬─────────┘
          │
          ▼
    ❌ BROKEN


┌─────────────────────────────────────────────────────────────────────────────┐
│  SADA (FIXED) ✅                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│                        Vercel Deployment                                  │
│                                                                           │
│   ┌─────────────────┐                                                    │
│   │   Frontend      │  Poziva: /api/services                            │
│   │   (Next.js)     │  Poziva: /api/categories                          │
│   │                 │  Poziva: /api/appointments                        │
│   └────────┬────────┘                                                    │
│            │                                                             │
│            │ Internal                                                    │
│            ▼                                                             │
│   ┌─────────────────┐                                                    │
│   │  API Routes     │                                                    │
│   │  /api/*         │                                                    │
│   │                 │                                                    │
│   │ • services      │                                                    │
│   │ • categories    │                                                    │
│   │ • appointments  │                                                    │
│   │ • availability  │                                                    │
│   │ • admin/...     │                                                    │
│   └────────┬────────┘                                                    │
│            │                                                             │
│            │ Prisma Client                                              │
│            ▼                                                             │
└────────────────────────────────────────────────────────────────────────────┘
             │
             │ Connection Pool
             │ (DATABASE_URL)
             ▼
┌────────────────────────┐
│    Neon Database       │
│    (PostgreSQL)        │
│                        │
│  • ServiceCategory     │
│  • Service             │
│  • Appointment         │
└────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│  ŠTO JE PROMIJENJENO                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Prije:
  • 2 zasebna deployment-a (client + server)
  • Frontend zove eksterni API preko NEXT_PUBLIC_API_URL
  • Server deployment ne postoji
  • Production broken

✅ Sada:
  • 1 deployment (samo client)
  • Frontend zove /api (Next.js API Routes)
  • Sve radi interno u istom deploymenu
  • Prisma direktno povezan na Neon
  • Brže (nema eksternih HTTP poziva)
  • Jeftinije (1 umjesto 2 deployments)


┌─────────────────────────────────────────────────────────────────────────────┐
│  API ENDPOINTS                                                               │
└─────────────────────────────────────────────────────────────────────────────┘

Public Endpoints:
  GET    /api/services              → Lista usluga
  GET    /api/categories            → Lista kategorija
  GET    /api/appointments          → Lista appointmenta
  POST   /api/appointments          → Kreiraj appointment
  GET    /api/availability          → Slobodni termini

Admin Endpoints:
  GET    /api/admin/appointments    → Admin lista appointmenta
  POST   /api/admin/appointments    → Admin kreiranje
  GET    /api/admin/appointments/[id]  → Pojedinačni appointment
  PATCH  /api/admin/appointments/[id]  → Update status
  DELETE /api/admin/appointments/[id]  → Obriši appointment


┌─────────────────────────────────────────────────────────────────────────────┐
│  ENVIRONMENT VARIABLES                                                       │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Prije (u Vercel):
  DATABASE_URL="postgresql://...?\r\n"        ← \r\n problem
  NEXT_PUBLIC_API_URL="https://..."          ← Više ne treba

✅ Sada (u Vercel):
  DATABASE_URL="postgresql://..."             ← Samo ovo!


┌─────────────────────────────────────────────────────────────────────────────┐
│  DEPLOYMENT FLOW                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

1. Git push → Vercel
2. Vercel detektuje Next.js projekt u /client
3. Build Command: npm install && npx prisma generate && npm run build
4. Prisma generira client based on schema.prisma
5. Next.js build-a sve (frontend + API routes)
6. Deploy sve zajedno
7. Runtime: Next.js server radi i frontend i API
8. Prisma se spaja na Neon sa DATABASE_URL
9. ✅ Sve radi!


╔══════════════════════════════════════════════════════════════════════════════╗
║  GOTOVO! 🎉                                                                  ║
║                                                                              ║
║  Slijedeći korak:                                                            ║
║  1. Dobij Neon DATABASE_URL → https://console.neon.tech/                    ║
║  2. Deploy na Vercel                                                         ║
║  3. Uživaj! 🚀                                                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

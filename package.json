{
  "name": "restoran-fullstack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm run start",
    "install:all": "cd client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^7.0.0"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0"
  }
}
